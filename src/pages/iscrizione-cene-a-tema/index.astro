---
import Layout from "../../layouts/Layout.astro";

// SEO Data - noindex per thank you page
const seoData = {
  title: "Iscrizione Confermata - TRE Grill + Pizzeria",
  description: "Grazie per esserti iscritto alla newsletter del Ristorante Pizzeria TRE. Riceverai in anteprima le informazioni sulle cene a tema.",
  robots: "noindex, nofollow",
  canonical: "https://www.ristorantepizzeriatre.it/iscrizione-cene-a-tema/",
};
---

<Layout 
  title={seoData.title}
  description={seoData.description}
  robots={seoData.robots}
  canonical={seoData.canonical}
>
  <!-- Hero Thank You -->
  <section class="hero-thankyou">
    <div class="container hero-thankyou-content">
      <h1>Iscrizione Confermata!</h1>
      <p>
        Grazie per esserti iscritto! Riceverai in anteprima le informazioni sulle<br />
        nostre cene a tema e sulle offerte speciali.
      </p>
      <a href="/#scopri-menu" class="btn btn-primary">Esplora il Menù</a>
    </div>
  </section>
</Layout>

<style>
  .hero-thankyou {
    color: var(--text-white);
    text-align: center;

    background-image:
      linear-gradient(rgba(0, 0, 0, 0.55), rgba(0, 0, 0, 0.55)),
      url('/img/iscrizione-cene-a-tema/grazie-per-la-richiesta-ristorante-pizzeria-tre-merate.webp');
    background-size: cover;
    background-position: center;

    min-height: clamp(500px, 70vh, 700px);
    display: flex;
    align-items: center;
    justify-content: center;

    padding: 5rem 0;
  }

  @media (max-width: 768px) {
    .hero-thankyou {
      min-height: clamp(450px, 65vh, 600px);
      padding: 4rem 0;
    }
  }

  .hero-thankyou-content {
    max-width: 800px;
  }

  .hero-thankyou h1 {
    font-size: 48px;
    font-weight: 600;
    font-style: italic;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--text-white);
  }

  @media (max-width: 768px) {
    .hero-thankyou h1 {
      font-size: 35px;
    }
  }

  .hero-thankyou p {
    font-size: 20px;
    line-height: 1.6;
    margin-bottom: 2.5rem;
    color: var(--text-gray);
  }

  @media (max-width: 768px) {
    .hero-thankyou p {
      font-size: 18px;
    }

    .hero-thankyou p br {
      display: none;
    }
  }
</style>